1、卡的生命周期定义：'0-白卡 1-测试期  2-沉默期  3-正使用  4-停机 5-销户  6-预约销户',
	卡的生命周期变更要有历史记录，并且记录时间点；
	卡的激活时间单独记录；
	卡的注销时间？这个好像无法取得准确时间，用注销表记录注销卡提交时间即可；
//2、卡的业务使用状态定义：用户在用？自用？vip用？库存预留？流量池在用？
//	op_state：1-
	
	
3、用户生命周期定义：（状态变更应有记录，并且记录原因：如欠费停机，充值开机）
	预生成用户（含线下渠道） -> 试用期用户(绑卡或激活？？？？重要) -> 正式充值用户 -> 欠费停机用户 -> 销号用户
	黑名单用户 -> 测试用户 -> vip（白名单）用户 -> 虚拟流量池用户？  
	a：用户创建：
		选择一、发货时创建【预用户】：实时跟踪卡的流量；（好处：便于统计用户转化情况，便于更细致的流量监控，便于提醒用户激活）
		选择二、仅在激活时创建正式用户：用户激活时根据订单查找渠道信息，同时创建用户及用户账户；（不激活的用户如何管理？）
		建议：预定义用户 ->  激活试用用户  ->  正式用户（充值用户）
	b：计费时间点：（商议）
		选择一：卡第一次使用（激活）时开始计费，即第一天使用流量开始计费；问题：虽然账号有钱（赠送），但一直无法扣费，并且这个卡如何处理？激活期到一定时间自动变为可用
		选择二：（注册）用户绑卡时赠送同时开始计费；并且容易统计销号卡；
	c：欠费停机用户：
	    低于3元提醒；
	    低于1.3元应提前停机？或者处于停机保号？-- 不好，应该让用户可以欠费停机；
	    对于欠费停机用户，应单独做表记录。便于激活用户以及处理欠费卡；（好像也不需要，有状态来表示）
	d：欠费停机1~2个月，自动销户：
		销户用户表？数据量小可以先以状态表示，不用单独存储
		卡销户记录？
		销户用户的公众号如何处理？
		销户用户的账号建议保留，没有卡也可以根据手机号保留账号，便于二次营销（一元购）
	e：换卡用户的处理
		
		
		
     